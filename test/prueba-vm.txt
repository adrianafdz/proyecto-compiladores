program prueba;


type objeto {
    def number : m, o;
    def string : s;

    func f1(): number {
        o = 123;
        call ret(o + 2);
    }
}

def number : n, p, q;
def string : s;
def objeto : ob;

func f2(number a, number b): number {
    a = a + 1;
    call ret(a + b);
}

main() {
    n = call ob:f1();
    call print(call to_string(n));
}

end;